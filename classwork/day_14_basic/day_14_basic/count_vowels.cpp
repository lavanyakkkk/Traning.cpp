#include <iostream>

using namespace std;
int main() {
	/*int count = 0;
	char ch;
	cout << "enter the string: " << endl;
	cin >> ch;
	for (int i = 0;i <= '/0';i++) {
		if (ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U' || ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u')
		{
			count+=i;
		}
		cout << "the count of vowels" << count << endl;
	}
	*/
	cout << INT_MAX << endl;
	cout << INT_MIN << endl;

	cout << LONG_MAX << endl;
	cout << ULONG_MAX << endl;
	cout << LLONG_MAX << endl;
	cout << ULLONG_MAX << endl;
	cout << FLT_MAX << endl;
	cout << DBL_MAX << endl;
	cout << CHAR_MAX << endl;



}