/*
#include <iostream>
#include <string>
using namespace std;
int main() {
	string str;
	cout << "enter the string: " << endl;
	getline(cin, str);
	int i;

	for (i = 0;i < str.length();i++)
	{
		if (str[i] == 'a' || str[i] == 'e' || str[i] == 'i' || str[i] == 'o' || str[i] == 'u' ||
			str[i] == 'A' || str[i] == 'E' || str[i] == 'I' || str[i] == 'O' || str[i] == 'U') {
			str[i] = '*';
		}

	}
	cout << "the astrick of vowel is: " << str << endl;
}
	
	//apple ---->*ppl*

	*/

//-------------------------------------------------------------------------------------------------------------------------//

//count the number of upper and lower case in string
/*
#include <iostream>
#include <string>
using namespace std;
int main() {
	string str;
	cout << "enter the string: " << endl;
	getline(cin, str);
	int count_lower = 0;
	int count_upper = 0;
	int i;
	for ( i = 0;i < str.length();i++) {
		if (islower(str[i])) {
			count_lower ++;
		}
		if (isupper(str[i])) {
			count_upper ++;
		}
	}
	cout << "the counting of uppercase and lowercase in string is: " << count_upper << "and" << count_lower << endl;
}
*/

//remove all the digits from string 

#include <iostream>
#include <string>
using namespace std;
int main() {

}

	


