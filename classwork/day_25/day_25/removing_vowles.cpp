/*
#include <iostream>
#include <string>
using namespace std;
int main() {
	string str;
	cout << "enter the string: " << endl;
	getline(cin, str);
	string result = "";
	int i;
	for ( i = 0;i < str.length();i++) {
		if (str[i] != 'a' && str[i] != 'e' && str[i] != 'i' && str[i] != 'o' && str[i] != 'u' &&
			str[i] != 'A' && str[i] != 'E' && str[i] != 'I' && str[i] != 'O' && str[i] != 'U') {
			result += str[i];
		}
	}
	cout << "the resulted removing of vowels is : " << result << endl;
}
*/
/*
#include <iostream>
#include <string>
using namespace std;
int main() {
	string str;
	cout << "enter the string: " << endl;//banana
	getline(cin, str);
	char ch;
	cout << "enter the character that u want to search: " << endl;
	cin >> ch;
	int i;
	int count = 0;
	for (i = 0;i < str.length();i++) {
		if (ch == str[i]) {
			count++;

		}
	}
	cout << "the given character as repeadetd :" << ch << "is:"<<count << endl;
}
*/

//hello world - hella world

